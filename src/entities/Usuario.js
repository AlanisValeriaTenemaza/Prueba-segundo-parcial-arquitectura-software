class Usuario {
  constructor({ id, nombre, email, capital_disponible }) {
    this.id = id;
    this.nombre = nombre;
    this.email = email;
    this.capital_disponible = capital_disponible;
  }
}

export default Usuario;
